# 🪙 Crypto Exchange — 실시간 암호화폐 거래 정보 플랫폼

> 업비트(Upbit) Public API와 WebSocket을 활용한 실시간 암호화폐 시세 조회 플랫폼  
> Clean Architecture 모델링 + Feature-Sliced Design(FSD)을 적용한 프론트엔드 아키텍처 설계

---

## 📌 프로젝트 소개

업비트 퍼블릭 API와 WebSocket을 활용하여 **코인 리스트 조회**, **코인 상세(실시간 호가, 캔들 차트)** 페이지를 구현한 프로젝트입니다.

클린 아키텍처의 계층 분리 원칙과 FSD(Feature-Sliced Design) 폴더 구조를 결합하여, 변경에 유연하고 유지보수성이 높은 프론트엔드 아키텍처를 설계했습니다.

---

## 🛠️ 기술 스택

| 구분 | 기술 |
|------|------|
| **Framework** | Next.js 14 (App Router) |
| **Language** | TypeScript |
| **상태 관리** | React Query (서버 상태), Recoil (클라이언트 상태) |
| **실시간 통신** | WebSocket (자체 소켓 라이브러리) |
| **스타일링** | SCSS Modules |
| **패키지 매니저** | pnpm |
| **아키텍처** | Clean Architecture + Feature-Sliced Design (FSD) |
| **API** | Upbit Public REST API, Upbit WebSocket API |

---

## 🚀 주요 기능

### 1. 코인 리스트 페이지
- 업비트 Public API를 통한 전체 마켓 코인 리스트 조회
- 실시간 시세 정보 표시 (현재가, 변동률, 거래대금)
- 마켓별 필터링 (KRW, BTC, USDT)

### 2. 코인 상세 페이지
- **실시간 호가(Orderbook)**: WebSocket을 통한 실시간 매수/매도 호가 표시
- **캔들 차트**: 분/일/주/월 단위 캔들 차트 조회
- **실시간 체결 내역**: WebSocket 기반 실시간 거래 체결 데이터 표시

### 3. 자체 소켓 라이브러리
- WebSocket 연결 관리 (자동 재연결, 에러 핸들링)
- 구독/해제 기반의 메시지 관리
- 다중 채널 동시 구독 지원

---

## 🏗️ 아키텍처 설계

### Clean Architecture 적용

클린 아키텍처의 계층 분리 원칙을 차용하여 데이터 흐름을 단방향으로 설계했습니다.

```
API (외부 데이터)
  ↓
Repository (Mapper를 통한 도메인 엔티티 변환)
  ↓
Service (비즈니스 로직 / 유스케이스)
  ↓
Feature / Page (UI 레이어)
```

- **API 계층**: 업비트 REST API / WebSocket 통신 담당
- **Repository 계층**: API 응답 데이터를 **Mapper**를 통해 **도메인 엔티티(Entity)** 로 변환하여 외부 데이터 형식에 대한 의존성을 제거
- **Service 계층**: 도메인 엔티티를 조합하여 유스케이스를 구현. Feature 및 Page 레이어에서 사용
- **UI 계층**: Feature/Page 슬라이스에서 Service를 호출하고, 도메인 모델 기반으로 화면을 구성

### Feature-Sliced Design (FSD) 폴더 구조

FSD 아키텍처를 통해 도메인 중심의 폴더 구조를 적용했습니다.

```
src/
├── app/              # 앱 초기화, 프로바이더, 라우터, 전역 설정
├── pages/            # 라우트별 페이지 컴포넌트
│   ├── coin-list/    # 코인 리스트 페이지
│   └── coin-detail/  # 코인 상세 페이지
├── features/         # 사용자 액션 기반 기능 단위
│   ├── orderbook/    # 실시간 호가 기능
│   └── chart/        # 캔들 차트 기능
├── entities/         # 비즈니스 도메인 모델링
│   ├── coin/         # 코인 도메인 (엔티티, 타입, API 어댑터)
│   ├── market/       # 마켓 도메인
│   └── ticker/       # 시세 도메인
├── shared/           # 공통 유틸리티, UI 컴포넌트, 소켓 라이브러리
│   ├── lib/          # 소켓 라이브러리, 유틸리티
│   ├── ui/           # 공통 UI 컴포넌트
│   └── api/          # API 클라이언트, 공통 인터페이스
```

### 레이어 간 의존성 규칙

```
app → pages → features → entities → shared
         (상위 → 하위 방향으로만 의존)
```

- **entities**: 비즈니스 개념(도메인)을 모델링. Repository, Mapper, 도메인 타입 정의
- **features / pages**: Service 레이어를 두고, 각 슬라이스(UI 및 기능 도메인)에서 유스케이스를 구현
- **shared**: 특정 비즈니스 로직에 종속되지 않은 범용 코드 (소켓 라이브러리 포함)

---

## 📐 주요 설계 패턴

### 1. Repository + Mapper 패턴

```typescript
// Entity (도메인 모델)
interface CoinEntity {
  code: string;
  name: string;
  currentPrice: number;
  changeRate: number;
}

// Mapper (API 응답 → 도메인 엔티티 변환)
const coinMapper = (raw: UpbitMarketResponse): CoinEntity => ({
  code: raw.market,
  name: raw.korean_name,
  currentPrice: raw.trade_price,
  changeRate: raw.signed_change_rate,
});

// Repository (데이터 접근 추상화)
class CoinRepository {
  async getCoins(): Promise<CoinEntity[]> {
    const response = await upbitApi.getMarkets();
    return response.map(coinMapper);
  }
}
```

### 2. Service 레이어 (유스케이스)

```typescript
// Service (비즈니스 로직 구현)
class CoinListService {
  constructor(private coinRepo: CoinRepository) {}

  async getCoinsByMarket(market: string): Promise<CoinEntity[]> {
    const coins = await this.coinRepo.getCoins();
    return coins.filter(coin => coin.code.startsWith(market));
  }
}
```

### 3. 소켓 라이브러리

```typescript
// WebSocket 매니저 (자체 구현)
class SocketManager {
  connect(url: string): void;
  subscribe(channel: string, callback: (data: unknown) => void): void;
  unsubscribe(channel: string): void;
  disconnect(): void;
}
```

---

## 🔑 주요 개발 내역

| 항목 | 설명 |
|------|------|
| **클린 아키텍처 도입** | API → Repository(Mapper) → Service → UI 계층 분리로 외부 API 변경에 유연한 구조 설계 |
| **FSD 폴더 구조 적용** | 도메인 중심의 코드 구성으로 코드 발견성(Discoverability) 향상 및 결합도 감소 |
| **자체 소켓 라이브러리 개발** | WebSocket 연결 관리, 자동 재연결, 다중 채널 구독을 지원하는 범용 소켓 라이브러리 구현 |
| **Mapper를 통한 도메인 격리** | API 응답 데이터를 도메인 엔티티로 변환하여 외부 의존성 제거 (Anti-Corruption Layer) |
| **서버/클라이언트 상태 분리** | React Query(서버 상태)와 Recoil(클라이언트 상태)을 조합한 명확한 상태 관리 전략 |
| **실시간 데이터 처리** | WebSocket 기반 실시간 호가, 시세, 체결 내역 스트리밍 구현 |

---

## 📂 프로젝트 구조 상세

```
src/
├── app/                          # 앱 설정 레이어
│   ├── providers/                # 전역 프로바이더 (QueryClient, Recoil, Socket)
│   ├── styles/                   # 전역 스타일
│   └── layout.tsx                # 루트 레이아웃
│
├── pages/                        # 페이지 레이어
│   ├── coin-list/
│   │   ├── ui/                   # 페이지 UI 컴포넌트
│   │   └── api/                  # 페이지 데이터 패칭
│   └── coin-detail/
│       ├── ui/                   # 상세 페이지 UI
│       └── api/                  # 상세 데이터 패칭
│
├── features/                     # 기능 레이어
│   ├── orderbook/
│   │   ├── ui/                   # 호가 UI 컴포넌트
│   │   ├── model/                # 호가 상태, 타입, 비즈니스 로직
│   │   └── lib/                  # 호가 유틸리티
│   └── chart/
│       ├── ui/                   # 차트 UI 컴포넌트
│       └── model/                # 차트 설정, 타입
│
├── entities/                     # 엔티티 레이어 (도메인 모델)
│   ├── coin/
│   │   ├── model/                # 코인 도메인 타입, 인터페이스
│   │   ├── api/                  # 코인 API 어댑터, Repository, Mapper
│   │   └── ui/                   # 코인 관련 공통 UI
│   ├── market/
│   │   ├── model/                # 마켓 도메인 타입
│   │   └── api/                  # 마켓 API 어댑터
│   └── ticker/
│       ├── model/                # 시세 도메인 타입
│       └── api/                  # 시세 API 어댑터
│
└── shared/                       # 공유 레이어
    ├── lib/
    │   ├── socket/               # 자체 소켓 라이브러리
    │   └── utils/                # 공통 유틸리티
    ├── ui/                       # 공통 UI 컴포넌트
    ├── api/                      # API 클라이언트 설정
    └── consts/                   # 공통 상수
```

---

## 🧩 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────┐
│                        Pages Layer                          │
│  ┌─────────────────┐    ┌──────────────────────────────┐    │
│  │   Coin List      │    │        Coin Detail            │    │
│  │   Page           │    │   (Orderbook + Chart)         │    │
│  └────────┬─────────┘    └──────────────┬────────────────┘    │
├───────────┼──────────────────────────────┼──────────────────┤
│           │        Features Layer        │                  │
│  ┌────────▼─────────┐    ┌──────────────▼────────────┐     │
│  │  Market Filter    │    │  Orderbook  │   Chart     │     │
│  │  Feature          │    │  Feature    │   Feature   │     │
│  └────────┬──────────┘    └──────┬──────┴─────┬───────┘     │
├───────────┼──────────────────────┼────────────┼─────────────┤
│           │       Entities Layer (Domain)     │             │
│  ┌────────▼──────────────────────▼────────────▼──────┐      │
│  │  Coin Entity  │  Market Entity  │  Ticker Entity   │      │
│  │  (model/api)  │  (model/api)    │  (model/api)     │      │
│  └────────┬──────┴────────┬───────┴───────┬──────────┘      │
├───────────┼───────────────┼───────────────┼─────────────────┤
│           │        Shared Layer           │                 │
│  ┌────────▼───────────────▼───────────────▼──────────┐      │
│  │  Socket Library  │  API Client  │  UI Components   │      │
│  └──────────────────┴──────────────┴─────────────────┘      │
├─────────────────────────────────────────────────────────────┤
│                  External Services                          │
│  ┌──────────────────────┐  ┌────────────────────────────┐   │
│  │  Upbit REST API       │  │  Upbit WebSocket API       │   │
│  │  (Markets, Candles)   │  │  (Ticker, Orderbook, Trade)│   │
│  └──────────────────────┘  └────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📝 학습 자료

이 레포지토리에는 프로젝트 개발 과정에서 학습한 기술 노트가 포함되어 있습니다.

| 주제 | 경로 |
|------|------|
| Clean Architecture | `소프트웨어 개발/클린 아키텍처/` |
| Feature-Sliced Design | `소프트웨어 개발/FSD (기능 분할 설계).md` |
| React 심화 | `react/` |
| TypeScript | `타입스크립트/` |
| Next.js | `next.js/` |
| React Query | `react-query/` |
| Recoil | `recoil/` |
| 디자인 패턴 | `소프트웨어 개발/디자인 패턴/` |